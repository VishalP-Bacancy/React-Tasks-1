Create an input component that has an input element and a button,
and another to display input data once the user clicks the button. 
If the user updates the data it should only reflect on the view component when the user clicks the button.